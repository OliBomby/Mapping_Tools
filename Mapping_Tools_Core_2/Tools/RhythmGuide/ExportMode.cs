namespace Mapping_Tools_Core.Tools.RhythmGuide {
    public enum ExportMode {
        /// <summary>
        /// Makes a new map with only the rhythm guide objects.
        /// </summary>
        NewMap,

        /// <summary>
        /// Adds the rhythm guide objects to an existing map.
        /// </summary>
        AddToMap,
    }
}