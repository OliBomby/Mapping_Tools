<UserControl x:Class="Mapping_Tools.Views.PropertyTransformerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mapping_Tools.Views"
             xmlns:cm="clr-namespace:System.ComponentModel;assembly=System"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Width="900" Height="700">
    <Grid Name="MasterGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="84"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Name="TitleGrid" Grid.Row="0">
            <TextBlock FontSize="40" TextWrapping="NoWrap" FontWeight="Bold" Foreground="#FF5F5E5E" Height="61" VerticalAlignment="Top"><Run Text="Property Transformer"/></TextBlock>
        </Grid>
        <Grid Name="FirstGrid" Grid.Row="1">
            <StackPanel>
                <StackPanel.Resources>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="FontSize" Value="24"/>
                    </Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Setter Property="FontSize" Value="24"/>
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="Timingpoints:" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New offset = Offset " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="TPOffsetMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="TPOffsetOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New BPM = BPM " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="TPBPMMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="TPBPMOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New slider velocity = SV " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="TPSVMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="TPSVOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New custom index = Index " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="TPIndexMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="TPIndexOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New volume = Volume " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="TPVolumeMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="TPVolumeOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <TextBlock Text=""/>
                <TextBlock Text="Other:" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New hitobject time = Time " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="HOTimeMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="HOTimeOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="New bookmark time = Time " MinWidth="350"/>
                    <TextBlock Text="x "/>
                    <TextBox x:Name="BookTimeMultiplierBox" Text="1" MinWidth="120"/>
                    <TextBlock Text=" + "/>
                    <TextBox x:Name="BookTimeOffsetBox" Text="0" MinWidth="120"/>
                </StackPanel>

                <StackPanel Margin="0,20,0,0">
                    <CheckBox x:Name="ClipBox" Content="Clip properties" ToolTip="Clips the parameters to stay within bounds. (Volume: 5-100 etc)" IsChecked="False" FontSize="20"/>
                    <CheckBox x:Name="FiltersBox" Content="Enable filters" IsChecked="False" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                    <StackPanel Visibility="{Binding Path=IsChecked, ElementName=FiltersBox, UpdateSourceTrigger=PropertyChanged,  
                                 Converter={StaticResource BooleanToVisibilityConverter}, 
                                 FallbackValue=Collapsed}">
                        <TextBox x:Name="MatchBox" Text="" ToolTip="Parameters must match this value to be changed." MinWidth="120"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="MinBox" Text="" ToolTip="Minimum time in milliseconds of the object for the parameters to be changed." MinWidth="120"/>
                            <TextBlock Text=" To " FontSize="20"/>
                            <TextBox x:Name="MaxBox" Text="" ToolTip="Maximum time in milliseconds of the object for the parameters to be changed." MinWidth="120"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <WrapPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Button Width="Auto" Height="Auto" x:Name="start" Cursor="Hand" Click="Start_Click" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}" Style="{StaticResource IconButton}">
                    <materialDesign:PackIcon Width="90" Height="90" Cursor="Hand" Kind="Play" Foreground="#FFD6D6D6"/>
                </Button >
            </WrapPanel>
        </Grid>
        <Grid Name="SecondGrid" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <WrapPanel VerticalAlignment="Bottom">
                    <TextBlock TextWrapping="NoWrap" Text="Made by OliBomby" FontWeight="Bold" Foreground="#FF5F5E5E"/>
                </WrapPanel>
            </Grid>
            <Grid Grid.Column="1">
                <ProgressBar x:Name="progress" Margin="20,0,20,0" Height="20" Foreground="#FF838383" Background="{x:Null}" BorderBrush="{x:Null}"/>
            </Grid>
        </Grid>
    </Grid>
    <UserControl.Resources>
        <cm:BackgroundWorker x:Key="backgroundWorker"
         DoWork="BackgroundWorker_DoWork"
         RunWorkerCompleted="BackgroundWorker_RunWorkerCompleted"
         WorkerReportsProgress="True"
         ProgressChanged="BackgroundWorker_ProgressChanged"/>
    </UserControl.Resources>
</UserControl>
