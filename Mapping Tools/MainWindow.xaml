<Window 
        StateChanged="Window_StateChanged"
        MouseDown="DragWin" 
        x:Class="Mapping_Tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cm="clr-namespace:System.ComponentModel;assembly=System"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:Mapping_Tools.views"
        xmlns:viewmodels="clr-namespace:Mapping_Tools.viewmodels"
        mc:Ignorable="d"
        Title="Beatmap Cleaner 2.0.0"  
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        ShowInTaskbar="True"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto" Background="{DynamicResource MaterialDesignPaper}"
        >
    <WindowChrome.WindowChrome>
        <WindowChrome 
        CaptionHeight="35"
        ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="FirstGrid" VerticalAlignment="Top" Grid.Column="0" Grid.Row="0">
            <StackPanel WindowChrome.IsHitTestVisibleInChrome="true" Height="35" VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                <Image Margin="10,0,0,0" HorizontalAlignment="Left" Source="data/map_cleaner.png" Height="35"/>
                <TextBlock HorizontalAlignment="Left" Height="35" Text="Mapping Tools" Foreground="#FF5F5E5E" Padding="0,13,0,0" Width="135"/>
            </StackPanel>
            <StackPanel WindowChrome.IsHitTestVisibleInChrome="true" Height="35" VerticalAlignment="Top" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Style="{DynamicResource MaterialDesignRaisedDarkButton}" Content="{materialDesign:PackIcon WindowMinimize}" Click="MinimizeWin" BorderBrush="{x:Null}" Background="{x:Null}" HorizontalAlignment="Right" Height="35" Panel.ZIndex="1" Foreground="#FFD6D6D6"/>
                <Button x:Name="toggle_button" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Content="{materialDesign:PackIcon WindowMaximize}" Click="ToggleWin" BorderBrush="{x:Null}" Background="{x:Null}" HorizontalAlignment="Right" Height="35" Foreground="#FFD6D6D6"/>
                <Button Style="{DynamicResource MaterialDesignRaisedDarkButton}" Content="{materialDesign:PackIcon WindowCLose}" Click="CloseWin" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Right"  Height="35" Foreground="#FFD6D6D6"/>
            </StackPanel>
        </Grid>
        <Grid x:Name="SecoundGrid" Grid.Column="0" Grid.Row="1">
            <Menu Foreground="#FFD6D6D6" >
                <MenuItem Header="_Tools" Padding="12,0,0,0" Height="31" MinWidth="65">
                    <MenuItem Header="_General Cleaner" Click="LoadCleaner"/>
                    <MenuItem Header="_Hitsound Copier" Click="LoadCopier"/>
                </MenuItem>
                <MenuItem Header="_Options" Padding="12,0,0,0" Height="31" MinWidth="80">
                    <MenuItem Header="_Preferences"/>
                    <MenuItem Header="_Open Backup Folder" Click="OpenBackups"/>
                </MenuItem>
                <MenuItem Header="_About" Padding="12,0,0,0" Height="31" MinWidth="67">
                    <MenuItem Header="_Github" Click="OpenGitHub"/>
                    <MenuItem Header="_Info" Click="OpenInfo"/>
                </MenuItem>
            </Menu>
        </Grid>

        <Grid x:Name="ThirdGrid" Grid.Row="2">
            <Border BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.MenuHighlightBrushKey}}">
                <ContentControl Content="{Binding}"></ContentControl>
            </Border>
        </Grid>
        <Grid x:Name="ForthGrid" Grid.Row="3" Margin="0,1,0,0" RenderTransformOrigin="0.5,0.5">
            <StackPanel WindowChrome.IsHitTestVisibleInChrome="true" Height="35" VerticalAlignment="Bottom" Orientation="Horizontal">
                <TextBlock Padding="10,10,0,0" HorizontalAlignment="Left" Height="35" Text="Current Tool:" Foreground="#FF5F5E5E"/>
                <TextBlock Name="currentTool" Padding="10,10,0,0" Height="35" Foreground="#FF5F5E5E"/>
            </StackPanel>
        </Grid>
    </Grid>
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:StandardVM}">
            <views:StandardView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:CleanerVM}">
            <views:CleanerView DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
</Window>