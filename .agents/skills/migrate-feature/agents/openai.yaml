interface:
  display_name: "Feature Migration"
  short_description: "Port one legacy feature without behavior drift"
  default_prompt: "Use $migrate-feature to migrate one bounded WPF feature while preserving its current-wave behavior and keeping both frontends buildable."
