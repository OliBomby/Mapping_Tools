{
  "stryker-config": {
    "project": "Mapping_Tools.Core.csproj",
    "mutate": [
      "**/*.cs",
      "!**/MathUtil/**/*.cs"
    ],
    "reporters": ["dots", "markdown", "json"],
    "thresholds": {
      "high": 80,
      "low": 60,
      "break": 0
    }
  }
}
