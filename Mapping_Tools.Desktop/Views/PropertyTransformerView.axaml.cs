using Avalonia.Controls;

namespace Mapping_Tools.Desktop.Views;

/// <summary>Displays the Avalonia Property Transformer form.</summary>
public partial class PropertyTransformerView : UserControl
{
    /// <summary>Creates the Property Transformer view and loads its compiled markup.</summary>
    public PropertyTransformerView()
    {
        InitializeComponent();
    }
}
